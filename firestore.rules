
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to all documents
    // for this simple, password-protected application.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
